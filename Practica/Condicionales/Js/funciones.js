//Funciones Tradicionales

function Positive() {
    const number = 0;

    if (number > 0) {
        console.log("El número", number, "es positivo");
    }
    else {
        console.log("El número", number, "es menor o igual a cero");
    }
}

Positive(); //Llamado de la funcion

/* Realice un programa que verifique si el user y el password son correctos.
Si son correctos mostrar Bienvenidos, en caso contrario mostrar Usuario y/o
Password incorrectos.
 */